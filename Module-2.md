# Docker Fundamentals & CLI

## จากวงจรชีวิตของคอนเทนเนอร์ (Container Lifecycle) คำสั่ง Docker พื้นฐาน:

### 1. เริ่มสร้าง (Create)

คำสั่งนี้ใช้สำหรับสร้างคอนเทนเนอร์ใหม่จาก Image แต่คอนเทนเนอร์จะยังไม่เริ่มต้นทำงาน (อยู่ในสถานะ Created)

```bash
docker create [OPTIONS] [IMAGE_NAME]

```

* **ตัวอย่าง:** `docker create --name my-web nginx` (สร้างคอนเทนเนอร์ชื่อ my-web จาก image nginx)

---

### 2. รัน (Start)

คำสั่งนี้ใช้สำหรับสั่งให้คอนเทนเนอร์ที่ถูกสร้างไว้แล้ว (หรือถูกหยุดไว้) เริ่มต้นทำงาน

```bash
docker start [CONTAINER_NAME_OR_ID]

```

* **ตัวอย่าง:** `docker start my-web`

> **💡 ข้อควรรู้เพิ่มเติม:** ในการใช้งานจริง ผู้คนมักนิยมใช้คำสั่ง `docker run` ซึ่งเป็นการรวบขั้นตอน **Create** และ **Start** ไว้ในคำสั่งเดียว เพื่อความรวดเร็ว
> * **ตัวอย่าง:** `docker run -d --name my-web nginx` (สร้างและรันคอนเทนเนอร์ในพื้นหลังทันที)
> 
> 

---

### 3. หยุดการทำงาน (Stop)

คำสั่งนี้ใช้สำหรับส่งสัญญาณให้คอนเทนเนอร์ที่กำลังทำงานอยู่ หยุดการทำงานอย่างปลอดภัย (เปลี่ยนเป็นสถานะ Exited)

```bash
docker stop [CONTAINER_NAME_OR_ID]

```

* **ตัวอย่าง:** `docker stop my-web`

---

### 4. ลบทำลาย (Remove)

คำสั่งนี้ใช้สำหรับลบคอนเทนเนอร์ออกจากระบบ เพื่อคืนพื้นที่และจัดการทรัพยากร (โดยปกติคอนเทนเนอร์จะต้องถูก Stop ก่อนจึงจะสามารถลบได้)

```bash
docker rm [CONTAINER_NAME_OR_ID]

```

* **ตัวอย่าง:** `docker rm my-web`
* **หากต้องการบังคับลบ (Force Remove)** คอนเทนเนอร์ที่กำลังทำงานอยู่ สามารถใช้ flag `-f` ได้: `docker rm -f my-web`



##  คำสั่งหลัก (Core Commands)

คำสั่งหลัก (Core Commands) ที่ใช้บ่อยในการจัดการ Docker ผ่าน Command Line (CLI):

### 1. `docker run`

คำสั่งนี้ใช้สำหรับ **สร้างและเริ่มต้นการทำงาน** ของคอนเทนเนอร์ใหม่จาก Image (เปรียบเสมือนการใช้ `docker create` และตามด้วย `docker start` ในคำสั่งเดียว)

```bash
docker run [OPTIONS] [IMAGE_NAME]

```

**ตัวอย่างและ Option ที่ใช้บ่อย:**

* `docker run nginx` : รันคอนเทนเนอร์จาก image ที่ชื่อ nginx
* `docker run -d nginx` : รันคอนเทนเนอร์ในโหมดพื้นหลัง (Detached mode)
* `docker run --name my-web nginx` : ตั้งชื่อคอนเทนเนอร์ว่า "my-web"
* `docker run -p 8080:80 nginx` : ทำการ Map Port จากเครื่องโฮสต์ (8080) ไปยังพอร์ตของคอนเทนเนอร์ (80)

---

### 2. `docker ps`

คำสั่งนี้ใช้สำหรับ **แสดงรายการคอนเทนเนอร์** เพื่อดูสถานะการทำงาน

```bash
docker ps [OPTIONS]

```

**ตัวอย่างและ Option ที่ใช้บ่อย:**

* `docker ps` : แสดงเฉพาะคอนเทนเนอร์ที่กำลังทำงานอยู่ (Running) ในปัจจุบัน
* `docker ps -a` : แสดงคอนเทนเนอร์ **ทั้งหมด** ทั้งที่กำลังทำงานอยู่และหยุดทำงานไปแล้ว (All)
* `docker ps -q` : แสดงเฉพาะ Container ID ของคอนเทนเนอร์ที่กำลังทำงานอยู่ (มักใช้ร่วมกับคำสั่งอื่นเพื่อจัดการทีละหลายๆ ตัว)

---

### 3. `docker rm`

คำสั่งนี้ใช้สำหรับ **ลบคอนเทนเนอร์** ออกจากระบบเพื่อคืนพื้นที่ (โดยปกติจะต้องหยุดการทำงานของคอนเทนเนอร์ก่อนจึงจะลบได้)

```bash
docker rm [OPTIONS] [CONTAINER_NAME_OR_ID]

```

**ตัวอย่างและ Option ที่ใช้บ่อย:**

* `docker rm my-web` : ลบคอนเทนเนอร์ที่ชื่อ my-web
* `docker rm -f my-web` : บังคับลบ (Force) คอนเทนเนอร์แม้ว่ามันจะกำลังทำงานอยู่ก็ตาม

---

### 4. การจัดการ Image พื้นฐาน

ตามที่รูปภาพระบุไว้ นอกจากการจัดการคอนเทนเนอร์แล้ว ยังมีคำสั่งพื้นฐานสำหรับการจัดการ Docker Image (ไฟล์ต้นแบบสำหรับสร้างคอนเทนเนอร์) ดังนี้ครับ:

* **`docker pull [IMAGE_NAME]`**
ใช้สำหรับดาวน์โหลด Image จาก Registry (เช่น Docker Hub) มาเก็บไว้ในเครื่อง
* *ตัวอย่าง:* `docker pull ubuntu:latest`


* **`docker images`** (หรือ `docker image ls`)
ใช้แสดงรายการ Image ทั้งหมดที่ถูกดาวน์โหลดหรือสร้างไว้ในเครื่องของคุณ
* **`docker rmi [IMAGE_NAME_OR_ID]`**
ใช้สำหรับลบ Image ออกจากเครื่อง (ลบได้ก็ต่อเมื่อไม่มีคอนเทนเนอร์ใดๆ ใช้งาน Image นั้นอยู่)
* *ตัวอย่าง:* `docker rmi nginx`



## **Operations & Logs**

**Operations & Logs** จะเกี่ยวข้องกับคำสั่งในการจัดการ ควบคุม และตรวจสอบการทำงานของคอนเทนเนอร์

### 1. การรันแอปพลิเคชันแบบ Background (Detached mode)

เมื่อต้องการรันคอนเทนเนอร์ให้ทำงานอยู่เบื้องหลัง โดยไม่ล็อกหน้าจอ Command Line ของเราไว้ จะใช้ flag `-d` (ย่อมาจาก detached) ร่วมกับคำสั่ง `docker run`

```bash
docker run -d [IMAGE_NAME]

```

* **ตัวอย่าง:** `docker run -d --name my-web nginx` (รัน Nginx ในโหมดพื้นหลังและตั้งชื่อว่า my-web)
* **ข้อสังเกต:** เมื่อรันคำสั่งเสร็จ ระบบจะคืนค่า Container ID แบบยาวมาให้ และเราสามารถพิมพ์คำสั่งอื่นต่อไปได้ทันที

---

### 2. การเข้าไปควบคุม (Exec)

คำสั่งนี้ใช้สำหรับเข้าไปรันคำสั่ง (Execute) หรือเปิด Shell ภายในคอนเทนเนอร์ที่กำลังทำงานอยู่ (Running) เพื่อเข้าไปจัดการไฟล์หรือตั้งค่าต่างๆ

```bash
docker exec -it [CONTAINER_NAME_OR_ID] [COMMAND]

```

* **ตัวอย่าง:** `docker exec -it my-web bash` หรือ `docker exec -it my-web sh` (เข้าไปเปิดใช้งาน shell ภายในคอนเทนเนอร์ my-web เพื่อพิมพ์คำสั่งต่างๆ เสมือนเราล็อกอินเข้าไปในเครื่องนั้น)
* **ความหมายของ flag ที่สำคัญ:**
* `-i` (interactive): ทำให้ Standard Input (STDIN) เปิดค้างไว้เพื่อให้เราพิมพ์โต้ตอบได้
* `-t` (tty): จำลอง Terminal ขึ้นมาให้เราใช้งาน



---

### 3. วิธีการเรียกดู Logs เพื่อแก้ปัญหา (Debug)

คำสั่งนี้ใช้สำหรับดูประวัติข้อความ หรือ Output ต่างๆ ที่แอปพลิเคชันภายในคอนเทนเนอร์แสดงผลออกมา ซึ่งมีประโยชน์มากในการค้นหาข้อผิดพลาด (Debug)

```bash
docker logs [OPTIONS] [CONTAINER_NAME_OR_ID]

```

**ตัวอย่างและ Option ที่ใช้บ่อย:**

* `docker logs my-web` : แสดง Logs ทั้งหมดตั้งแต่เริ่มต้นรันคอนเทนเนอร์ my-web
* `docker logs -f my-web` : การใช้ flag `-f` (follow) จะเป็นการเกาะดู Logs แบบเรียลไทม์ (เหมือนคำสั่ง `tail -f` ใน Linux) หน้าจอจะอัปเดตอัตโนมัติเมื่อมี Log ใหม่เข้ามา
* `docker logs --tail 50 my-web` : แสดง Logs เฉพาะ 50 บรรทัดล่าสุด เพื่อไม่ให้ข้อมูลเยอะจนเกินไป